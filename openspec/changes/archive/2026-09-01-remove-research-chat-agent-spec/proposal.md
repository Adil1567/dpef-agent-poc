## Why

`openspec/specs/research-chat-agent/spec.md` describes the `research_chat_agent` LangGraph example, which was already deleted from the repo (a prior cleanup removed the code but missed this main spec). The spec now describes code that no longer exists.

## What Changes

- Remove the `research-chat-agent` capability entirely from `openspec/specs/`.

## Capabilities

### New Capabilities
(none)

### Modified Capabilities
(none - this is a full removal, not a requirement change)

## Impact

- Affects: `openspec/specs/research-chat-agent/` (spec removed entirely). No code impact - the code was already deleted.
